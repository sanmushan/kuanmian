<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="@android:color/transparent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/count_down"
        android:layout_width="166px"
        android:layout_height="65px"
        android:layout_marginLeft="31px"
        android:layout_marginTop="25px"
        android:background="@drawable/count_down_bg"
        android:drawableLeft="@drawable/count_down_back"
        android:drawablePadding="-25px"
        android:gravity="center"
        android:paddingLeft="15px"
        android:textColor="@color/count_down_tv"
        android:textSize="@dimen/text_count_down"
        tools:text="100'" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center">

        <TextView
            android:id="@+id/tv_noodle_news_title"
            android:visibility="gone"
            android:layout_width="1000px"
            android:layout_height="80px"
            android:background="@drawable/tv_tiele_bg"
            android:gravity="center"
            android:textColor="@color/white"
            android:textSize="30px" />
    </LinearLayout>
</LinearLayout>